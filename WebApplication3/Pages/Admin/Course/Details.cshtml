@page
@model WebApplication3.Pages.Admin.Course.DetailsModel

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>

    <div>
        <h4>Courses</h4>
        <hr />
        <dl class="row">
            <!-- Исправлено: убраны лишние угловые скобки -->
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Courses.title)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Courses.title)
            </dd>

            <!-- Исправлена опечатка в названии свойства -->
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Courses.desceiption)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Courses.desceiption)
            </dd>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Courses.price)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Courses.price)
            </dd>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Courses.discount)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Courses.discount)
            </dd>

            <!-- Исправлен синтаксис для TeacherId -->
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Courses.TeacherId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Courses.TeacherId)
            </dd>
        </dl>
    </div>

    <div>
        <a asp-page="./Edit" asp-route-id="@Model.Courses.id">Edit</a> |
        <a asp-page="./Index">Back to List</a>
    </div>
</body>
</html>